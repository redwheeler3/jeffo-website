{
  "name": "jeffo-website",
  "private": true,
  "version": "1.0.0",
  "license": "MIT",
  "homepage": "https://www.jeffo.net",
  "scripts": {
    "build": "node scripts/build.mjs",
    "prepreview": "npm run build",
    "preview": "node scripts/preview.mjs",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}
